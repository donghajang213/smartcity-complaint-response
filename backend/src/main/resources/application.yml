spring:
  datasource:
    url: jdbc:postgresql://13.124.223.79:5432/smartcity_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver   # ← 드라이버 클래스 명시

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect   # ← 방언 추가

server:
  port: 8080

