spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartcity_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver   # ✅ 추가

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect   # ✅ 추가
external:
  fastapi:
    url: http://localhost:8001/chat  # 또는 테스트용 목업 주소