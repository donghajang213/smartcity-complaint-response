spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartcity_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver   # ← 드라이버 클래스 명시

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect   # ← 방언 추가

server:
  port: 8080

external:
  fastapi:
    url: http://fastapi:8001/chat

recaptcha:
  secret: 6Ld1lmArAAAAAGvarEbptb3-i1gnutfvF3L2w0ub

kakao:
  rest-api-key: 8413a4aab24c3a25cc81eb161bcd7277

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

ad:
  # AD_UPLOAD_DIR 환경 변수가 없으면 /app/uploads/ads 로,
  # 있으면 그 값을 사용합니다.
  upload-dir: ${AD_UPLOAD_DIR:/app/uploads/ads}

  # AD_BASE_URL 환경 변수가 없으면 아래 URL을,
  # 있으면 그 값을 사용합니다.
  base-url:   ${AD_BASE_URL:https://smartcityksva.site/api/ads}

servlet:
  multipart:
    max-file-size: 20MB
    max-request-size: 20MB